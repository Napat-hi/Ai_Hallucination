# Jupyter
.ipynb_checkpoints/

# Python
__pycache__/
*.pyc
.venv/
venv/
env/

# Env / secrets
.env
*.token

# Model weights, HF cache, datasets, pipeline outputs
# (this project downloads/generates these into /workspace on the GPU pod;
# never commit them if a run ever happens to write into this repo folder)
models/
hf_cache/
*.safetensors
*.bin
*.pt
*.npy
explanations*.npy
results*.json

# Vendored private dependency (natural_language_autoencoders), see modal_app.py
vendor/

# Modal
.modal/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
